---
title: '文件系统'
date: '2022-06-01'
updated: '2024-07-11'
description: '文件 io image.pngassets/文件系统-1.png 这里要注意的一点是：文件的 io 其实可以分为两个步骤 数据在内核中准备好（也就是说从网络或者是磁盘先拷贝到内核缓冲区），从内核空间拷贝到用户进程缓冲区。 所有的同步 io，'
published: true
category: '后端学习'
tags: ['计算机基础', '操作系统']
---

# 文件 IO

![image.png](/images/posts/计算机基础-操作系统-文件系统/文件系统-1.png)

这里要注意的一点是：文件的 IO 其实可以分为两个步骤 数据在内核中准备好（也就是说从网络或者是磁盘先拷贝到内核缓冲区），从内核空间拷贝到用户进程缓冲区。

所有的同步 IO，不管是阻塞、非阻塞、IO 多路复用，在第二步（从内核空间拷贝到用户进程缓冲区）这个过程都是同步的，都是需要等待的。区别是：阻塞 IO 是第一步和第二步都等待，非阻塞 IO 是第一步不等待，但是会一直轮询内核数据准备好了没，而 IO 多路复用是使用一个线程阻塞监听，内核数据准备好了就通知对应线程。但归根结底，这些方法在第二个阶段都是同步的！

只有异步 IO 才是真的两个阶段都不同步，等数据拷贝到用户空间才处理。

# 文件 IO

![image.png](/images/posts/计算机基础-操作系统-文件系统/文件系统-1.png)

这里要注意的一点是：文件的 IO 其实可以分为两个步骤 数据在内核中准备好（也就是说从网络或者是磁盘先拷贝到内核缓冲区），从内核空间拷贝到用户进程缓冲区。

所有的同步 IO，不管是阻塞、非阻塞、IO 多路复用，在第二步（从内核空间拷贝到用户进程缓冲区）这个过程都是同步的，都是需要等待的。区别是：阻塞 IO 是第一步和第二步都等待，非阻塞 IO 是第一步不等待，但是会一直轮询内核数据准备好了没，而 IO 多路复用是使用一个线程阻塞监听，内核数据准备好了就通知对应线程。但归根结底，这些方法在第二个阶段都是同步的！

只有异步 IO 才是真的两个阶段都不同步，等数据拷贝到用户空间才处理。
