---
title: '哈希'
date: '2025-03-14'
updated: '2025-03-14'
description: '第一个是两数之和，很直接哈。 第二个是字母异位词分组。主要是哈希的key如何构造。一种想法是字符串排序作为key，另一种想法是计数，即 abcc的key为：a1b1c2。 第三个是最长连续序列。较暴力地来看应该是枚举每一个起点，判断是否存在'
published: true
category: '前端学习'
tags: ['算法刷题']
---

第一个是两数之和，很直接哈。

第二个是字母异位词分组。主要是哈希的key如何构造。一种想法是字符串排序作为key，另一种想法是计数，即

abcc的key为：a1b1c2。

第三个是最长连续序列。较暴力地来看应该是枚举每一个起点，判断是否存在x+1，x+2, ...，但是事实上并不需要遍历每一个，这样会存在很多重复计算，我们只需要遍历每个连续段的开头即可，也就是x-1没有的位置，这样总时间复杂度就是On了。

​
