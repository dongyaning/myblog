---
title: '基础'
date: '2023-04-24'
updated: '2023-04-24'
description: '上的内容 jdk jre jvm image.pngassets/基础-1.png 解释和编译共存，针对热点代码优化： image.pngassets/基础-2.png 基本类型和包装类型 包装类型的缓存机制，六种基本数据类型，除了浮点型都'
published: true
category: '后端学习'
tags: ['Java']
---

# 上的内容

JDK > JRE > JVM

![image.png](/images/posts/后端学习-java-基础/基础-1.png)

解释和编译共存，针对热点代码优化：

![image.png](/images/posts/后端学习-java-基础/基础-2.png)

基本类型和包装类型

包装类型的缓存机制，六种基本数据类型，除了浮点型都有缓存，范围是-127~128，布尔是两个值。

自动装箱和拆箱：

```java
Integer i = 10;  // 装箱
int n = i;   // 拆箱
```

从字节码中，我们发现装箱其实就是调用了 包装类的`valueOf()`方法，拆箱其实就是调用了 `xxxValue()`方法。

因此，

- `Integer i = 10` 等价于 `Integer i = Integer.valueOf(10)`
- `int n = i` 等价于 `int n = i.intValue()`;

频繁拆装箱会影响性能，所以尽量基本数据类型就写基本数据类型。

浮点数会丢失精度，需要精确用BigDecimal，

这里说一下BigDecimal，BigDecimal基于BigInt，就是在BigInt的基础上加了一个小数位的位置scale。

BigInt的内部是一个int[]，二进制存储大数，每个数组元素存储32位二进制，运算过程也相当于是模拟手工运算，要处理进位啥的。

补一下各种数据类型的特点：

| **变量类型**                          | **存储位置** | **默认值**           | **初始化时机**                   | **初始化主体** | **生命周期**  | **线程安全**             | **备注**                                  |
| ------------------------------------- | ------------ | -------------------- | -------------------------------- | -------------- | ------------- | ------------------------ | ----------------------------------------- |
| **局部变量**                          | 栈           | ❌ 无                | 声明后显式赋值                   | 开发者         | 方法/块执行期 | ✅ 天然安全              | 必须初始化                                |
| **实例变量**                          | 堆           | ✅ 有                | 对象创建时（构造器前）           | JVM → 开发者   | 对象存活期    | ❌                       | 可被 `final`&lt;br>/`volatile`&lt;br>修饰 |
| **静态变量**                          | 方法区       | ✅ 有                | 类初始化（`&lt;clinit>`&lt;br>） | JVM → 开发者   | 类加载到卸载  | `&lt;clinit>`&lt;br>安全 | `static final`&lt;br>常量特殊处理         |
| `static final`&lt;br>**\*\***常量\*\* | 常量池       | 直接赋值             | 类加载准备阶段                   | JVM            | 类加载到卸载  | ✅                       | 编译期内联                                |
| `final`&lt;br>**\*\***成员\*\*        | 堆           | ✅（先默认，后显式） | 构造器结束前                     | 开发者         | 对象存活期    | ✅（JMM 保证可见）       | 安全发布                                  |
| `volatile`&lt;br>**\*\***变量\*\*     | 堆/方法区    | ✅ 有                | 同普通变量                       | 同普通变量     | 同普通变量    | ✅（可见性）             | 禁止重排序                                |
| **数组元素**                          | 堆           | ✅ 有                | `new`&lt;br>时                   | JVM → 开发者   | 数组存活期    | ❌                       | 数组是对象                                |

重载和重写：

重载：多个方法有不同的参数，子类也可以重载父类方法（可见的话，父类方法也相当于是子类方法）。

重写：子类对父类的允许访问的方法的实现过程进行重新编写，方法名和参数列表必须完全相同。子类方法返回值类型应比父类方法返回值类型更小或相等，抛出的异常范围小于等于父类，访问修饰符范围大于等于父类。如果父类方法访问修饰符为 `private/final/static` 则子类就不能重写该方法。

# 面向对象（中）

不写构造方法会生成无参，但是写了有参的就不会生成无参了，写有参尽量要把无参也写出来，即使不用。

构造方法不能重写，但可以重载。

三大特性：封装、继承、多态。

接口和抽象类：

关于接口和抽象类的区别，我觉得最本质的区别还是接口是接口，抽象类是个类，即使是包含抽象方法的。接口和类本质上的设计目的就是不一样的，接口就是为了行为约束，做规范。抽象类就是为了代码复用，体现继承的所属关系。这是本质区别，不在语法细节。

接口在Java8之前方法全都是public abstarct，java 8 引入default和static，java9引入private（给default用的）。

深拷贝和浅拷贝：

- **浅拷贝**：浅拷贝会在堆上创建一个新的对象（区别于引用拷贝的一点），不过，如果原对象内部的属性是引用类型的话，浅拷贝会直接复制内部对象的引用地址，也就是说拷贝对象和原对象共用同一个内部对象。

- 也就是说**浅拷贝只是内部的引用不会新建，但是第一层的那个原始对象还是会新建的**。

- **深拷贝**：深拷贝会完全复制整个对象，包括这个对象所包含的内部对象。
- **引用拷贝**：两个不同引用指向同一个对象。

Object：常见方法

\==和equals()：

\==：基本数据类型比较值，引用数据类型比较引用

equals：

Object里面的：

```java
public boolean equals(Object obj) {
     return (this == obj);
}   // 所以这个和 == 是一样的（没重写equals的话）
```

重写了的情况一般就会比较对象里面的值了，比如String就重写了。

hashCode()：

获取哈希码，为什么要有哈希？

hashCode和equals差不多，都可以用来判断两个对象是不是一个，关系如下：

- 如果两个对象的`hashCode` 值相等，那这两个对象不一定相等（哈希碰撞）。
- 如果两个对象的`hashCode` 值相等并且`equals()`方法也返回 `true`，我们才认为这两个对象相等。
- 如果两个对象的`hashCode` 值不相等，我们就可以直接认为这两个对象不相等。

为什么不只用equals是因为在一些hashset中hash的效率更高。为什么不只用hash是因为hash一样，不一定值一样，因为会hash crash.

重写equals也要重写hashcode，因为要保证：equals相等，hashcode也相等。hash决定桶，而equals在桶内确定具体元素（hash list）。

String家族：

Stirng不可变，String Builder线程不安全，String Buffer线程安全（加了同步）。

**对于三者使用的总结：**

- 操作少量的数据: 适用 `String`
- 单线程操作字符串缓冲区下操作大量数据: 适用 `StringBuilder`
- 多线程操作字符串缓冲区下操作大量数据: 适用 `StringBuffer`

String不可变本质：数组（Java9从char[]改为byte[]）为private final且没有暴露出修改内容的方法。

String 之间的 + 是重载过的，是通过新建一个StringBuilder，调用append，最后 toString 实现的。

String.intern()

```java
String str1 = "str";
String str2 = "ing";
String str3 = "str" + "ing";
String str4 = str1 + str2;
String str5 = "string";
System.out.println(str3 == str4);//false
System.out.println(str3 == str5);//true
System.out.println(str4 == str5);//false

java 会对编译器可以确定内容的常量字符串折叠，如str3 会优化为 "string"
但是str4这样的变量运算，就会新建一个对象：
String str4 = new StringBuilder().append(str1).append(str2).toString();
```

**异常**

结构图了解

![image.png](/images/posts/后端学习-java-基础/基础-3.png)

Checked & Unchecked: Checked一定要处理，不然编译不过。Unchecked就是运行时异常。

try-catch-finnaly，finnally里面的return 会覆盖try里面的。

finnally代码块不是一定被执行：

- 调用 System.exit() 终止虚拟机
- 程序所在的线程死亡
- 关闭CPU

打开资源场景使用 try-with-resources

**反射**

注解

序列化：

**序列化**（serialization）在计算机科学的数据处理中，是指将数据结构或对象状态转换成可取用格式（例如存成文件，存于缓冲，或经由网络中发送），以留待后续在相同或另一台计算机环境中，能恢复原先状态的过程。依照序列化格式重新获取字节的结果时，可以利用它来产生与原始对象相同语义的副本。对于许多对象，像是使用大量引用的复杂对象，这种序列化重建的过程并不容易。面向对象中的对象序列化，并不概括之前原始对象所关系的函数。这种过程也称为对象编组（marshalling）。从一系列字节提取数据结构的反向操作，是反序列化（也称为解编组、deserialization、unmarshalling）。

综上：**序列化的主要目的是通过网络传输对象或者说是将对象存储到文件系统、数据库、内存中。**

对于不想进行序列化的变量，使用 `transient` 关键字修饰。

`**transient**` **关键字的作用是：阻止实例中那些用此关键字修饰的的变量序列化；当对象被反序列化时，被** `**transient**` **修饰的变量值不会被持久化和恢复。**

序列化就相当于是网络协议，编解码过程，JAVA自带的序列化协议不好，一般不用。不能跨语言，性能差，存在安全问题。

**SPI**

![image.png](/images/posts/后端学习-java-基础/基础-4.png)

SPI 实际上就是 Java 定义的一套规范和机制，和自动装配类似，JDK 或者 xx 框架定义了一个接口，并且 JDK 或者 xx 框架会自动加载某特定配置文件，加载到实现了这些接口的类就会将其作为对应接口的实现类。实现”框架（JDK 或其他的什么 Java 框架）定义接口，用户提供实现。“

JDK 内部也会定义一些接口，通过 ServiceLoader 来加载对应的三方实现，比如 java.sql.Driver。

自己定义的或者其它的三方框架，也可以自己定义接口，让框架使用者来按照规则提供实现。

**​**
