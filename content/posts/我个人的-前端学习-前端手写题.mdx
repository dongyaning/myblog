---
title: '前端手写题'
date: '2024-12-22'
updated: '2024-12-22'
description: '防抖节流 防抖 要注意 this 绑定到 return 的那个函数上，这个很重要，内部用箭头函数。 javascript const debounce = fn, delay=300 = { let timer = null; return'
published: true
category: '前端学习'
tags: []
---

# 防抖节流

## 防抖

要注意 this 绑定到 return 的那个函数上，这个很重要，内部用箭头函数。

```javascript
const debounce = (fn, delay = 300) => {
  let timer = null

  return function (...args) {
    if (timer) clearTimeout(timer)
    timer = setTimeout(() => {
      fn.apply(this, args)
    }, delay)
  }
}
```

## 节流

```javascript
// 两种写法，推荐第一种，因为第一次点击就会被执行
// 1.时间戳方案
const throttle = (fn, delay = 300) => {
    let lastTime = 0;

    return function(...args) {
        const currTime = Date.now();
        if (currTime - lastTime >= delay) {
            fn.apply(this, args);
            lastTime = currTime;
        }
    }
}

// 2.定时器方案（第一次执行需要等待delay时间）
const throttle = (fn, delay = 300) => {
    let timer = null;

    return function(...args) {
        if (timer) return ;
        timer = setTimeout(() => {
            fn.apply(this, args);
            timer = null;
        }, delay);
    }
}
```

# 组合寄生继承

```javascript
function Parent() {
  this.xx = xx;
  ...
}

function Child() {
  // 先继承父类的属性
  Parent.call(this)

  this.xx = xx;
}

// 维护原型链
Child.prototype = Object.create(Parent.prototype);
Child.prototype.constructor = Parent;
```

# Promise 并发控制

即实现一个调度器，控制同时处于 Pending 状态的异步任务数量。

```javascript
class Scheduler {
  constructor(limit) {
    this.limit = limit // 最大并发数
    this.runningCount = 0 // 当前正在运行的任务数
    this.queue = [] // 等待队列
  }

  add(promiseCreator) {
    return new Promise((resolve) => {
      // 将任务包装并放入队列
      this.queue.push(() => promiseCreator().then(resolve))
      this.run()
    })
  }

  run() {
    // 关键逻辑：如果没满且队列有活，就一直掏出来执行
    while (this.runningCount < this.limit && this.queue.length > 0) {
      const task = this.queue.shift()
      this.runningCount++

      task().finally(() => {
        this.runningCount--
        this.run() // 任务完成后，递归调用 run 尝试执行下一个
      })
    }
  }
}
```

# Promise.all()

```javascript
Promise.myAll = function (promises) {
  let count = 0
  const results = []
  const len = promises.length

  return new Promise((resolve, reject) => {
    promises.forEach((p, i) => {
      Promise.resolve(p)
        .then((res) => {
          results[count++] = res
          if (count == len) resolve(results)
        })
        .catch((err) => reject(err))
    })
  })
}
```

# Promise.race()
